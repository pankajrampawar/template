import { Karma, Inter, Kings, Karla } from "next/font/google";

export const karla = Karla({ subsets: ['latin'], weight: '400'})
export const karma = Karma({ subsets: ['latin'], weight: '700'})
export const inter = Inter({ subsets: ["latin"] });
export const kings = Kings({ subsets: ['latin'], weight: '400' })